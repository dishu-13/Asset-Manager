services:
  - type: web
    name: autohire-ai-api
    runtime: docker
    dockerfilePath: ./Dockerfile.api
    envVars:
      - key: PORT
        value: 8080
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/healthz
